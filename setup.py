from setuptools import setup, find_packages

setup(
    name="codebase",  # Your package name
    version="0.1",
    packages=find_packages(),
    install_requires=[],  # Add dependencies if needed
)
